specVersion: "v1"
serviceId: "proyecto"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "proyecto-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
